type: object
description: Provides a collection of the Entities configured in your instance and the metadata for each Entity.
properties:
  entities:
    type: array
    minItems: 1
    maxItems: 50
    description: Array of Rights Entity Objects.
    items:
      $ref: ../schemas/entity.yaml
  pagination:
    $ref: pagination.yaml