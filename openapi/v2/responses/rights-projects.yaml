type: object
description: Provides a collection of the Projects configured in your instance and the metadata for each Project.
properties:
  projects:
    type: array
    minItems: 1
    maxItems: 50
    description: Array of Rights Projects.
    items:
      $ref: ../schemas/project.yaml
  pagination:
    $ref: pagination.yaml