name: "rights"
in: "query"
description: The rights management object type.  For example, rights=entity, when you only want to return rights management Entities. Query parameters are always case insensitive.
schema: 
  type: "string"
  x-nullable: false
  required:
    - rights
  maxLength: 24
  minLength: 5
  example: entity
  enum:
    - project
    - entity
    - agreement