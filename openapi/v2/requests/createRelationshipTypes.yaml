type: array
maxItems: 50
minItems: 1
items:
  type: object
  properties:
    name:
      type: string
      minLength: 3
      maxLength: 50
      description: The name of the Relationship type you are creating.  Will be displayed to users unless a separate value for label is provided.
      example: derivative
    label: 
      type: string
      minLength: 3
      maxLength: 50
      description: Administrators with the Content Management role privilege can provides an alternative label for the relationship type to be shown to users.
      example: alternative
